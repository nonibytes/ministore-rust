[package]
name = "ministore-ffi"
version = "0.1.0"
edition = "2021"

[lib]
name = "ministore_ffi"
crate-type = ["cdylib"]

[dependencies]
ministore = { path = "../ministore" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
